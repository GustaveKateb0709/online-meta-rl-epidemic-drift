# Tested on Python 3.13 with the versions pinned below, CPU only.
# For a CPU-only PyTorch build install it explicitly first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

numpy>=2.4,<3
scipy>=1.18,<2
torch>=2.13,<3
matplotlib>=3.11,<4
pandas>=3.0,<4
scikit-learn>=1.9,<2
Pillow>=12.0,<13

# Only needed to build the manuscript .docx from the markdown source.
python-docx>=1.2,<2
